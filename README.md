# ShoutlyAI Migrated

Monorepo migration of the Outstand-powered ShoutlyAI demo.

| Path | Role |
|---|---|
| `backend/` | NestJS API on **http://localhost:3000** |
| `frontend/` | React (Vite) — expect **http://localhost:5173** |

Backend CORS is enabled for `http://localhost:5173`. See `backend/README.md` for API endpoints and setup.
