export function Header() {
  return (
    <header className="page-header">
      <span className="badge">SHOUTLY AI</span>
      <h1>Connect a Facebook Page &amp; Post</h1>
      <p className="muted">
        Runs against the Nest API (Outstand-backed). Your API key stays on the server.
      </p>
    </header>
  );
}
